<Window
    x:Class="MinecraftLocalizer.Views.LogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:MinecraftLocalizer.Behaviors"
    xmlns:converters="clr-namespace:MinecraftLocalizer.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:MinecraftLocalizer.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:properties="clr-namespace:MinecraftLocalizer.Properties"
    xmlns:viewModels="clr-namespace:MinecraftLocalizer.ViewModels"
    Title="Installing GPT4Free"
    Width="800"
    Height="600"
    MinWidth="600"
    MinHeight="400"
    d:DataContext="{d:DesignInstance Type=viewModels:LogViewModel}"
    Background="#111820"
    ResizeMode="CanResize"
    Style="{StaticResource WindowStyle}"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">



    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/ButtonStyle.xaml" />
                <ResourceDictionary Source="/Styles/SystemButtonsStyle.xaml" />
                <ResourceDictionary Source="/Styles/ScrollBarStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:BooleanConverter x:Key="BooleanConverter" />

            <Style x:Key="TitleTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="White" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Margin" Value="0,0,0,10" />
            </Style>

            <Style x:Key="StatusTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#CCCCCC" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <Style x:Key="ConsoleOutputStyle" TargetType="TextBox">
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Background" Value="#161D27" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="FontFamily" Value="Consolas, Courier New, monospace" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="IsReadOnly" Value="True" />
                <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
                <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
                <Setter Property="TextWrapping" Value="NoWrap" />
                <Setter Property="AcceptsReturn" Value="True" />
                <Setter Property="AcceptsTab" Value="True" />
            </Style>

            <Style x:Key="ProgressBarStyle" TargetType="ProgressBar">
                <Setter Property="Foreground" Value="#0CAF60" />
                <Setter Property="Background" Value="#2A3B50" />
                <Setter Property="Height" Value="10" />
                <Setter Property="Margin" Value="0,5,0,0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ProgressBar">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                <Grid x:Name="PART_Track">
                                    <Rectangle
                                        x:Name="PART_Indicator"
                                        HorizontalAlignment="Left"
                                        Fill="{TemplateBinding Foreground}"
                                        RadiusX="4"
                                        RadiusY="4" />
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#111820" CornerRadius="5">

        <i:Interaction.Behaviors>
            <behaviors:DragWindowBehavior />
        </i:Interaction.Behaviors>

        <Grid Margin="20,20,20,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Style="{StaticResource TitleTextStyle}"
                Text="{x:Static properties:Resources.InstallingGpt4Free}" />

            <StackPanel
                Grid.Row="0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Orientation="Horizontal">

                <Button Command="{Binding CloseCommand}" Style="{StaticResource CloseButtonStyle}" />
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,15">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        FontSize="14"
                        Style="{StaticResource StatusTextStyle}"
                        Text="{Binding Status}" />

                    <TextBlock
                        Grid.Column="0"
                        Margin="10,0,0,0"
                        FontSize="14"
                        Style="{StaticResource StatusTextStyle}"
                        Text="{Binding Progress, StringFormat={}{0:F0}%}" />
                </StackPanel>

                <ProgressBar
                    Grid.Row="1"
                    Grid.ColumnSpan="2"
                    Margin="0,10,0,0"
                    Maximum="100"
                    Style="{StaticResource ProgressBarStyle}"
                    Value="{Binding Progress}" />
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,-10,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                </Grid.ColumnDefinitions>

                <Border
                    Grid.Column="0"
                    Background="#161D27"
                    CornerRadius="5">

                    <TextBox
                        x:Name="ConsoleOutputTextBox"
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        AcceptsReturn="True"
                        AcceptsTab="True"
                        Background="#161D27"
                        BorderThickness="0"
                        FontSize="12"
                        Foreground="White"
                        HorizontalScrollBarVisibility="Hidden"
                        IsReadOnly="True"
                        Text="{Binding Output, Mode=OneWay}"
                        TextChanged="ConsoleOutputTextBox_TextChanged"
                        TextWrapping="NoWrap"
                        VerticalScrollBarVisibility="Hidden" />
                </Border>

                <ScrollBar
                    x:Name="CustomConsoleScrollBar"
                    Grid.Column="1"
                    Margin="3,5,-5,5"
                    Padding="0"
                    Style="{StaticResource ScrollBarStyle}"
                    ValueChanged="CustomConsoleScrollBar_ValueChanged" />
            </Grid>

            <TextBlock
                Grid.Row="3"
                Margin="0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                FontSize="12"
                Foreground="#888888"
                Text="{x:Static properties:Resources.Gpt4FreeInstallationWarning}"
                TextWrapping="Wrap" />

            <Button
                Grid.Row="3"
                Width="120"
                HorizontalAlignment="Right"
                Command="{Binding CancelCommand}"
                Content="{x:Static properties:Resources.Cancel}"
                IsEnabled="{Binding IsRunning}"
                Style="{StaticResource ButtonStyle}" />
        </Grid>
    </Border>
</Window>
